interface:
  display_name: "Adaptive Agent Orchestrator"
  short_description: "Context-efficient bounded agent orchestration"
  default_prompt: "Use $adaptive-agent-orchestrator only for genuinely independent workstreams; minimize copied context and dispatch progressively."

policy:
  allow_implicit_invocation: true
